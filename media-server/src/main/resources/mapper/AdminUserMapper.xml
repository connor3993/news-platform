<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.connor.newsplatform.server.mapper.AdminUserMapper">

    <!-- 新增管理员 -->
    <insert id="insert" parameterType="AdminUser" useGeneratedKeys="true" keyProperty="id">
        insert into admin_user (username, password, name, phone, role, status, create_time, update_time, create_user, update_user)
        values (#{username}, #{password}, #{name}, #{phone}, #{role}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <!-- 修改管理员（动态SQL，只更新非空字段） -->
    <update id="update" parameterType="AdminUser">
        update admin_user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser}</if>
        </set>
        where id = #{id}
    </update>

</mapper>
