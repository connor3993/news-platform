# 2026-06-26 工作日志

## Mapper 接口注解化改造
- 按照截图风格（Javadoc + @Select/@Insert/@Update/@Delete 注解），改造了全部 13 个 Mapper 接口
- 为每个接口添加了类级别 Javadoc 注释
- 为每个方法添加了详细的 Javadoc 说明
- 将简单 SQL 从 XML 迁移到注解中（约 20+ 个 SQL 语句）
- 复杂动态 SQL（含 `<if>`/`<where>`/`<choose>`/`<foreach>`）保留在 XML 中
- 清理了 XML 文件，移除已迁移的语句，保留动态 SQL 和必要的注释
