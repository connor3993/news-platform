## 2026-06-25

- 修复 JWT 过期弹出一堆技术性错误的问题：后端 `GlobalExceptionHandler` 对 JWT 异常返回 `code=401, msg=登录已过期，请重新登录`；管理端 `admin/src/utils/request.js` 和用户端 `user/src/utils/request.js` 在响应拦截器里检测到 401/403 时直接清 token 并跳转登录页，不再显示原始异常信息。后端已重新编译通过。
- 将整个后端从 MyBatis-Plus 改写为纯 MyBatis（参照苍穹外卖风格）：pom.xml 去掉 MP 依赖换 mybatis-spring-boot-starter；11 个 Entity 去掉 @TableName/@TableId；12 个 Mapper 接口去掉 BaseMapper 改为显式方法；新建 12 个 Mapper XML 写 SQL；4 个 ServiceImpl 去掉 LambdaQueryWrapper 改用 PageHelper + mapper 调用；删除 MyBatisPlusConfig；ArticleServiceImpl 从 700 行简化到 ~450 行，去掉文章级 Redis 缓存；编译通过。


- 重写 `ArticleCard.vue`：改为垂直卡片（图片上方 + 标题/摘要/元信息下方），添加渐变色占位图；桌面端首张大卡片占 2 列，形成 Bento Grid 头条区。
- 搜索页去掉右侧蓝色搜索按钮，改为无边框一体化搜索框（回车搜索）。
- 确认文章分类功能正常显示（全部/科技/财经/传媒/国际），分类页可正常筛选。
- 重新构建前端并启动后端验证，页面布局已彻底改变。
